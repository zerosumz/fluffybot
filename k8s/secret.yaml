# 실제 배포 시 값 채워서 apply
# kubectl create secret generic fluffybot-secrets -n gitlab \
#   --from-literal=gitlab-token=glpat-xxxx \
#   --from-literal=anthropic-api-key=sk-ant-xxxx
apiVersion: v1
kind: Secret
metadata:
  name: fluffybot-secrets
  namespace: gitlab
type: Opaque
stringData:
  gitlab-token: "REPLACE_WITH_GITLAB_PAT"
  anthropic-api-key: "REPLACE_WITH_ANTHROPIC_KEY"
